on:
  push:
    branches:
      - main
    paths:
      - '**.go'
  workflow_dispatch: {}

jobs:
  test:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-go@v2
      - run: make test
